-- Leading comment
CREATE CACHED TABLE sql_file_schema (
  schema_name VARCHAR(32) NOT NULL PRIMARY KEY,
);
-- Interstatement comment
CREATE CACHED TABLE sql_file_schema (
  schema_version INT NOT NULL
);
-- Trailing comment
